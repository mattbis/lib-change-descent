
/** identify a host runtime, and host machine id when saving session information */
// TODO (matt): this should call subroutine for each major os line: nix, win, osx
export function host_fingerprint() {
    const fingerprint= {
      
    }
    return fingerprint= {}
}

/** return a load float of system load */
export function host_metric_load() /* :flot */ {
    
}

/** return object containing:-
timeseries true|false
metric mem|load|disk_queue
duration 1s
interval 500ms
*/
export function host_metric(options= {time_series, metric, duration, interval}) {
    
}
