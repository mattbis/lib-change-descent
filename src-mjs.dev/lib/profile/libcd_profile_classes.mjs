export const PROFILE_VOL_PROFILE_CLASS= {
  // fixed, / mark as ...                   // cannot be fixed and removable
  // removable, / mark as volatile          // can be removable and temporary
  // temporary / mark as less important     // can be fixed or removable
}
