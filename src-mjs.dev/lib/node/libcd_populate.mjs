/** a profile modification causes the below - unless used in another way... */

// -- we don't rebuild the db, but as its processed if the link / path is stale
// its moved into another bucket....

/// TODO (matt): buckets... 

/** the session records counts and hashes */
export function populate_fst_counthash() {}

/** augment session or trigger with much slower seek */
export function populate_slw_size() {}

