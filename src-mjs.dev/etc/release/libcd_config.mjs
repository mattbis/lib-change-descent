/**
 * static release configuration - not an evolving .0. structure
 */

// 0x118CD (hexspeak for 'libcd')
// used as a user-space marker for disk imprint ownership manifests without tripping endpoint security
export const LIBCD_IMPRINT_MAGIC = 0x118CD

// OS journal binary packet magic header
export const LIBCD_JOURNAL_MAGIC = 0xCDCD
