/** expose main functions that allow different ways to consume the library */
export function main(profile= 'bg') {
    switch(profile) {
        case 'bg':
            break
        case 'fg':
            break
        default:
            break
    }
}
