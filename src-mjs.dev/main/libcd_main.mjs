/** expose main functions that allow different ways to consume the library */
export funciton main(profileMod) {
}
export function _main_switch(profileMod= 'bg') {
    // TODO (matt): something like
    profileMod.split(/\+/gi).forEach(m => {
        switch(profile) {
            case 'bg':
                break
            case 'fg':
                break
            default:
                break
        }
    }    
}
