// experimental... since this probably could bring down controllers and the entire master bus.. 
// it happens often with a certain SSD 
// yet the SSD is fine.. however it would be handy to see if this is feasible

// however, there are orders of magnitude with media, and its no longer a reliable method
// since modern very high end hdds spin fast and can read fast, in NTFS, there are conditions where
// both ssd and hdd are slow to write many small files.... since each file has a big object associated with it
// and you have the variance implied by virus scanning software.. which adds a unknown amount of time
